// Q6 Stripping Names Task :
// Store the person's name with whitespace
var personName = "\t   Ghulam Hyder \n";
// Print the name with whitespace
console.log("Name with whitespace:", personName);
// Print the name after stripping whitespaces
var trimmedName = personName.trim();
console.log("Name after stripping whitespaces:", trimmedName);
